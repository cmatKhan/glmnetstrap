% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_cv_glmnet.R
\name{bootstrap_cv_glmnet}
\alias{bootstrap_cv_glmnet}
\title{Bootstrap Multiple cv.glmnet Models with Resampled Weights}
\usage{
bootstrap_cv_glmnet(n_bootstraps, seed = NULL, ...)
}
\arguments{
\item{n_bootstraps}{Number of bootstrap replicates.}

\item{seed}{Optional integer seed. When provided, ensures repeatability
across replicates by offsetting the seed for each bootstrap
(i.e., \code{seed + i}). If \code{NULL}, results are different each time.}

\item{...}{
  Arguments passed on to \code{\link[=fit_cv_glmnet]{fit_cv_glmnet}}
  \describe{
    \item{\code{x}}{A numeric matrix of predictors (samples x features). Must have
column names.}
    \item{\code{y}}{A numeric response vector of length equal to the number of rows
in \code{x}.}
    \item{\code{fold_classes}}{Optional vector of class labels (same length as \code{y})
used to stratify the cross-validation folds. If \code{NULL}, glmnet will
handle CV splitting. If this is provided, \code{nfolds} must also be passed}
    \item{\code{active_predictors}}{Optional logical or numeric index vector indicating
which predictors to include in the model. If \code{NULL}, all predictors are
used. If logical, must be the same length as \code{ncol(x)}. This argument
is intended to be used in an interative process to drop out predictors}
  }}
}
\value{
A list of \code{cv.glmnet} objects, one per bootstrap replicate.
}
\description{
Performs repeated bootstrapped Lasso (or Elastic Net) modeling by generating
bootstrap weights and calling \code{fit_cv_glmnet()} for each replicate.
}
